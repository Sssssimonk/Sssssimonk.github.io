<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>关于这个博客</title>
    <link href="/2026/07/01/about-this-blog/"/>
    <url>/2026/07/01/about-this-blog/</url>
    
    <content type="html"><![CDATA[<p>这个博客会主要记录我对 AI 论文、算法思想和工程实践的理解。</p><p>我希望每篇文章都尽量回答三个问题：</p><ol><li>这篇论文或技术真正解决了什么问题？</li><li>它的核心假设、方法设计和实验结论是否站得住？</li><li>如果把它放到真实系统或后续研究里，它给我的启发是什么？</li></ol><p>后续内容会以论文阅读、方法拆解、实验复现、工程经验和研究想法为主。相比只摘录结论，我会更重视自己的判断：哪里简单有效，哪里可能被过度包装，哪里值得继续追。</p><p>一个简单的公式示例：</p><p>$$<br>\mathcal{L}(\theta) &#x3D; - \sum_{t&#x3D;1}^{T} \log p_\theta(y_t \mid y_{&lt;t}, x)<br>$$</p><p>一个代码块示例：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">def</span> <span class="hljs-title function_">summarize_claim</span>(<span class="hljs-params">paper</span>):<br>    <span class="hljs-keyword">return</span> &#123;<br>        <span class="hljs-string">&quot;problem&quot;</span>: paper.problem,<br>        <span class="hljs-string">&quot;method&quot;</span>: paper.method,<br>        <span class="hljs-string">&quot;evidence&quot;</span>: paper.experiments,<br>        <span class="hljs-string">&quot;my_take&quot;</span>: paper.limitations,<br>    &#125;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>Blog</category>
      
    </categories>
    
    
    <tags>
      
      <tag>AI</tag>
      
      <tag>Research</tag>
      
      <tag>Notes</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>机器学习笔记 06：评估、验证与模型选择</title>
    <link href="/2024/07/14/ml-notes-06-evaluation-validation-model-selection/"/>
    <url>/2024/07/14/ml-notes-06-evaluation-validation-model-selection/</url>
    
    <content type="html"><![CDATA[<p>这一篇整理模型评估、验证和模型选择。训练 loss 下降只能说明模型更适合训练数据，不等于模型真的有用。评估指标和验证方式决定了最后会选择哪个模型，也决定了这个选择是否可靠。</p><h2 id="1-评估指标不是装饰"><a href="#1-评估指标不是装饰" class="headerlink" title="1. 评估指标不是装饰"></a>1. 评估指标不是装饰</h2><p>模型评估的核心问题是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">这个模型在真正关心的场景里，到底有没有变好？<br></code></pre></td></tr></table></figure><p>不同任务里，“好”的定义不一样。房价预测关心预测误差，垃圾邮件识别关心别把正常邮件误删，疾病筛查关心别漏掉阳性病例。</p><p>所以指标不是越多越好，而是要对应任务代价。</p><h2 id="2-回归指标（regression-metrics）"><a href="#2-回归指标（regression-metrics）" class="headerlink" title="2. 回归指标（regression metrics）"></a>2. 回归指标（regression metrics）</h2><p>回归任务预测连续值，常见指标有 MSE、RMSE、MAE、$R^2$。</p><h3 id="MSE-和-RMSE"><a href="#MSE-和-RMSE" class="headerlink" title="MSE 和 RMSE"></a>MSE 和 RMSE</h3><p>MSE：</p><p>$$<br>MSE&#x3D;\frac{1}{n}\sum_{i&#x3D;1}^{n}(y_i-\hat{y}_i)^2<br>$$</p><p>RMSE：</p><p>$$<br>RMSE&#x3D;\sqrt{MSE}<br>$$</p><p>MSE 对大误差更敏感，因为误差被平方。RMSE 和原始目标变量单位一致，更容易解释。</p><p>比如预测房价时，RMSE &#x3D; 20 万，比 MSE &#x3D; 400 亿更直观。</p><h3 id="MAE"><a href="#MAE" class="headerlink" title="MAE"></a>MAE</h3><p>MAE：</p><p>$$<br>MAE&#x3D;\frac{1}{n}\sum_{i&#x3D;1}^{n}|y_i-\hat{y}_i|<br>$$</p><p>MAE 对 outlier 没有 MSE 那么敏感。</p><p>如果数据里有少数极端样本，比如一两套超级豪宅，MSE 会被这些样本强烈影响，MAE 会更稳一些。</p><h3 id="R-2"><a href="#R-2" class="headerlink" title="$R^2$"></a>$R^2$</h3><p>$R^2$ 衡量模型解释了多少目标变量的 variance：</p><p>$$<br>R^2&#x3D;1-\frac{\sum_i(y_i-\hat{y}_i)^2}{\sum_i(y_i-\bar{y})^2}<br>$$</p><p>如果 $R^2&#x3D;0.8$，可以粗略理解为模型解释了 80% 的变化。它适合看整体拟合程度，但不一定能说明具体业务误差是否可接受。</p><h2 id="3-分类指标（classification-metrics）"><a href="#3-分类指标（classification-metrics）" class="headerlink" title="3. 分类指标（classification metrics）"></a>3. 分类指标（classification metrics）</h2><p>分类任务常见指标都可以从 confusion matrix 出发。</p><table><thead><tr><th></th><th align="right">Predicted Positive</th><th align="right">Predicted Negative</th></tr></thead><tbody><tr><td>Actual Positive</td><td align="right">TP</td><td align="right">FN</td></tr><tr><td>Actual Negative</td><td align="right">FP</td><td align="right">TN</td></tr></tbody></table><p>TP 是真正例，FP 是假正例，FN 是假负例，TN 是真负例。</p><h3 id="准确率（accuracy）"><a href="#准确率（accuracy）" class="headerlink" title="准确率（accuracy）"></a>准确率（accuracy）</h3><p>$$<br>Accuracy&#x3D;\frac{TP+TN}{TP+TN+FP+FN}<br>$$</p><p>Accuracy 直观，但在 imbalanced dataset 上容易误导。</p><p>比如一个疾病数据集中，99% 都是阴性。模型全部预测阴性，也有 99% accuracy，但它完全找不到阳性病人。</p><h3 id="精确率（precision）"><a href="#精确率（precision）" class="headerlink" title="精确率（precision）"></a>精确率（precision）</h3><p>$$<br>Precision&#x3D;\frac{TP}{TP+FP}<br>$$</p><p>Precision 关心预测为正的样本里，有多少是真的正。</p><p>垃圾邮件识别里 precision 很重要，因为把正常邮件误判成垃圾邮件会影响用户。</p><h3 id="召回率（recall）"><a href="#召回率（recall）" class="headerlink" title="召回率（recall）"></a>召回率（recall）</h3><p>$$<br>Recall&#x3D;\frac{TP}{TP+FN}<br>$$</p><p>Recall 关心真实正样本里，有多少被找出来。</p><p>疾病筛查里 recall 很重要，因为漏诊的代价可能很高。</p><h3 id="F1-分数（F1-score）"><a href="#F1-分数（F1-score）" class="headerlink" title="F1 分数（F1 score）"></a>F1 分数（F1 score）</h3><p>$$<br>F1&#x3D;2\cdot\frac{Precision\cdot Recall}{Precision+Recall}<br>$$</p><p>F1 是 precision 和 recall 的调和平均。它适合在两者都重要时作为综合指标。</p><p>但 F1 也不是万能的。如果业务上 FP 和 FN 代价明显不同，还是要单独看 precision 和 recall。</p><h2 id="4-ROC-和-AUC：阈值变化下的分类表现"><a href="#4-ROC-和-AUC：阈值变化下的分类表现" class="headerlink" title="4. ROC 和 AUC：阈值变化下的分类表现"></a>4. ROC 和 AUC：阈值变化下的分类表现</h2><p>ROC 曲线关注不同 threshold 下 TPR 和 FPR 的变化。</p><p>$$<br>TPR&#x3D;\frac{TP}{TP+FN}<br>$$</p><p>$$<br>FPR&#x3D;\frac{FP}{FP+TN}<br>$$</p><p>AUC 是 ROC 曲线下面积。它也可以理解为：随机抽一个正样本和一个负样本，模型把正样本排在负样本前面的概率。</p><p>这就是为什么 AUC 常用于排序、推荐、广告等场景。很多时候模型输出的不是最终类别，而是一个 score，业务再根据 threshold 或排序位置使用这个 score。</p><p>一个小例子：</p><table><thead><tr><th>用户</th><th align="right">是否点击</th><th align="right">模型 score</th></tr></thead><tbody><tr><td>A</td><td align="right">1</td><td align="right">0.90</td></tr><tr><td>B</td><td align="right">0</td><td align="right">0.80</td></tr><tr><td>C</td><td align="right">1</td><td align="right">0.70</td></tr><tr><td>D</td><td align="right">0</td><td align="right">0.20</td></tr></tbody></table><p>AUC 关心的是正样本是否整体排在负样本前面，而不是某个固定 threshold 下分对了多少。</p><h2 id="5-PR-曲线（PR-curve）"><a href="#5-PR-曲线（PR-curve）" class="headerlink" title="5. PR 曲线（PR curve）"></a>5. PR 曲线（PR curve）</h2><p>PR Curve 的横轴是 recall，纵轴是 precision。</p><p>在正负样本极度不平衡时，PR curve 往往比 ROC curve 更敏感。</p><p>比如欺诈检测里，正样本非常少。ROC-AUC 可能看起来还不错，但 precision 可能很低：模型找出一堆“疑似欺诈”，里面真正欺诈的比例很小。这个时候 PR curve 更能反映实际使用体验。</p><h2 id="6-类别不平衡数据集（imbalanced-dataset）"><a href="#6-类别不平衡数据集（imbalanced-dataset）" class="headerlink" title="6. 类别不平衡数据集（imbalanced dataset）"></a>6. 类别不平衡数据集（imbalanced dataset）</h2><p>类别不平衡时，accuracy 通常不够。</p><p>常见处理方式：</p><ul><li>换指标：precision、recall、F1、AUC、PR-AUC</li><li>重采样：oversampling &#x2F; undersampling</li><li>调整 loss：给少数类更高权重</li><li>调 threshold：根据业务代价调整预测阈值</li><li>anomaly detection：把少数类当异常点处理</li></ul><p>一个例子：阳性率只有 1% 的疾病筛查。模型如果全部预测阴性，accuracy 是 99%，但 recall 是 0。这种模型没有实际价值。</p><h2 id="7-验证（validation）：怎么可靠地估计泛化能力"><a href="#7-验证（validation）：怎么可靠地估计泛化能力" class="headerlink" title="7. 验证（validation）：怎么可靠地估计泛化能力"></a>7. 验证（validation）：怎么可靠地估计泛化能力</h2><p>训练集表现不能代表泛化能力，所以需要 validation。</p><h3 id="留出法验证（holdout-validation）"><a href="#留出法验证（holdout-validation）" class="headerlink" title="留出法验证（holdout validation）"></a>留出法验证（holdout validation）</h3><p>最常见方式是 train &#x2F; validation &#x2F; test split。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">training set: train model parameters<br>validation set: tune hyperparameters and choose models<br>test set: final evaluation<br></code></pre></td></tr></table></figure><p>test set 不应该频繁用来调模型。如果每次调参都看 test score，test set 也会被间接过拟合。</p><h3 id="K-折交叉验证（K-fold-cross-validation）"><a href="#K-折交叉验证（K-fold-cross-validation）" class="headerlink" title="K 折交叉验证（K-fold cross validation）"></a>K 折交叉验证（K-fold cross validation）</h3><p>K-fold cross validation 把数据分成 $K$ 份，每次用其中一份做 validation，其余做 training，最后平均结果。</p><p>它比单次 holdout 更稳定，尤其适合数据量不大的情况。</p><p>缺点是训练成本更高。比如 5-fold 就要训练 5 次。</p><h3 id="Bootstrap"><a href="#Bootstrap" class="headerlink" title="Bootstrap"></a>Bootstrap</h3><p>Bootstrap 是有放回采样。每次从原始数据中采出一个同样大小的数据集，有些样本会重复，有些样本不会被采到。没被采到的样本可以用于评估。</p><p>它在统计估计里很常见，但日常模型训练中更常用 holdout 和 K-fold。</p><h2 id="8-模型选择（model-selection）"><a href="#8-模型选择（model-selection）" class="headerlink" title="8. 模型选择（model selection）"></a>8. 模型选择（model selection）</h2><p>Model selection 不只是选模型类型，也包括选超参数。</p><p>比如：</p><ul><li>Linear Regression vs Random Forest vs XGBoost</li><li>Decision tree 的 <code>max_depth</code></li><li>KNN 的 <code>K</code></li><li>Logistic regression 的 regularization strength</li><li>XGBoost 的 learning rate、tree depth、number of estimators</li></ul><h3 id="网格搜索（grid-search）"><a href="#网格搜索（grid-search）" class="headerlink" title="网格搜索（grid search）"></a>网格搜索（grid search）</h3><p>Grid search 枚举所有组合。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">max_depth = [3, 5, 7]<br>learning_rate = [0.01, 0.1]<br></code></pre></td></tr></table></figure><p>总共会试 $3 \times 2 &#x3D; 6$ 种组合。</p><p>优点是简单直接，缺点是组合多时成本很高。</p><h3 id="随机搜索（random-search）"><a href="#随机搜索（random-search）" class="headerlink" title="随机搜索（random search）"></a>随机搜索（random search）</h3><p>Random search 从参数空间中随机采样。它经常比 grid search 更高效，因为并不是所有超参数都同等重要。</p><p>比如 10 个超参数里真正敏感的可能只有 2 个，grid search 会浪费很多组合，random search 反而可能更快碰到好区域。</p><h3 id="贝叶斯优化（Bayesian-optimization）"><a href="#贝叶斯优化（Bayesian-optimization）" class="headerlink" title="贝叶斯优化（Bayesian optimization）"></a>贝叶斯优化（Bayesian optimization）</h3><p>Bayesian optimization 会根据已有实验结果，估计哪些参数区域更可能好，然后优先尝试这些区域。</p><p>它比 random search 更聪明，但实现和调试更复杂。小任务里不一定需要，上规模后才更有价值。</p><h2 id="9-数据泄漏（data-leakage）"><a href="#9-数据泄漏（data-leakage）" class="headerlink" title="9. 数据泄漏（data leakage）"></a>9. 数据泄漏（data leakage）</h2><p>Data leakage 是评估里最危险的问题之一。它指训练过程中用了不该提前知道的信息。</p><p>例子：预测用户未来是否流失，但 feature 里包含“用户是否已经取消订阅”。这个特征在预测时根本不可用，但训练时会让模型表现非常好。</p><p>另一个例子：先对全量数据做 normalization，再划分 train&#x2F;test。这样 test set 的统计信息已经泄漏到了 training process。正确做法是只在 training set 上 fit scaler，再 apply 到 validation&#x2F;test set。</p><p>Data leakage 会让离线评估虚高，线上效果崩掉。</p><h2 id="10-几个点"><a href="#10-几个点" class="headerlink" title="10. 几个点"></a>10. 几个点</h2><p>Accuracy 不适合严重类别不平衡的问题。</p><p>Precision 和 recall 要结合 FP&#x2F;FN 的业务代价看，不要机械比较谁更大。</p><p>AUC 更像排序指标，适合看 score 的整体区分能力。</p><p>PR curve 在正样本稀少时更敏感。</p><p>Validation set 用来选模型，test set 留到最后做最终评估。</p><p>数据泄漏比模型小 bug 更可怕，因为它会让结果看起来特别好。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://scikit-learn.org/stable/model_selection.html">scikit-learn User Guide: Model Selection and Evaluation</a></li><li><a href="https://scikit-learn.org/stable/modules/model_evaluation.html">scikit-learn User Guide: Metrics and Scoring</a></li><li><a href="https://www.statlearning.com/">An Introduction to Statistical Learning</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>机器学习笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>机器学习</tag>
      
      <tag>笔记</tag>
      
      <tag>评估</tag>
      
      <tag>交叉验证</tag>
      
      <tag>模型选择</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>机器学习笔记 05：无监督学习与降维</title>
    <link href="/2024/07/06/ml-notes-05-unsupervised-learning-dimensionality-reduction/"/>
    <url>/2024/07/06/ml-notes-05-unsupervised-learning-dimensionality-reduction/</url>
    
    <content type="html"><![CDATA[<p>这一篇整理无监督学习和降维。监督学习有 label，模型知道自己要预测什么；无监督学习没有明确答案，目标更多是发现数据结构。最常见的两类任务是 clustering 和 dimensionality reduction。</p><h2 id="1-什么是无监督学习（unsupervised-learning）"><a href="#1-什么是无监督学习（unsupervised-learning）" class="headerlink" title="1. 什么是无监督学习（unsupervised learning）"></a>1. 什么是无监督学习（unsupervised learning）</h2><p>无监督学习只有输入 $x$，没有标签 $y$。</p><p>比如有一批用户行为数据：</p><table><thead><tr><th>用户</th><th align="right">访问次数</th><th align="right">平均停留时间</th><th align="right">购买次数</th></tr></thead><tbody><tr><td>A</td><td align="right">50</td><td align="right">8 分钟</td><td align="right">5</td></tr><tr><td>B</td><td align="right">3</td><td align="right">1 分钟</td><td align="right">0</td></tr><tr><td>C</td><td align="right">45</td><td align="right">7 分钟</td><td align="right">4</td></tr><tr><td>D</td><td align="right">5</td><td align="right">2 分钟</td><td align="right">0</td></tr></tbody></table><p>没有人告诉模型谁是“高价值用户”，但模型可以发现 A 和 C 比较像，B 和 D 比较像。这就是 clustering 的典型使用场景。</p><p>无监督学习不是为了直接预测 label，而是为了整理数据：哪些样本相似，数据有没有低维结构，有没有异常点。</p><h2 id="2-聚类（clustering）：把相似样本放在一起"><a href="#2-聚类（clustering）：把相似样本放在一起" class="headerlink" title="2. 聚类（clustering）：把相似样本放在一起"></a>2. 聚类（clustering）：把相似样本放在一起</h2><p>Clustering 的目标是把样本分成若干组，让同一组内部尽量相似，不同组之间尽量不同。</p><p>但这里有一个麻烦：没有 label，所以“分得好不好”不像分类任务那样容易判断。</p><p>不同 clustering 方法对 cluster 形状有不同假设。KMeans 偏向球形 cluster，DBSCAN 偏向密度连通区域，hierarchical clustering 更强调层级关系。</p><h2 id="3-KMeans：最经典的聚类方法"><a href="#3-KMeans：最经典的聚类方法" class="headerlink" title="3. KMeans：最经典的聚类方法"></a>3. KMeans：最经典的聚类方法</h2><p>KMeans 是最经典的 clustering 方法之一。它需要先指定 cluster 数量 $K$。</p><p>基本流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs text">choose K initial centers<br>assign each point to the nearest center<br>update each center by averaging assigned points<br>repeat until centers stop changing<br></code></pre></td></tr></table></figure><p>目标可以理解为最小化簇内平方距离：</p><p>$$<br>\sum_{k&#x3D;1}^{K}\sum_{x_i \in C_k}|x_i-\mu_k|^2<br>$$</p><p>其中，$C_k$ 是第 $k$ 个 cluster，$\mu_k$ 是这个 cluster 的中心。</p><p>一个简单例子：如果用户按“访问次数”和“购买次数”分布明显，一群是高访问高购买，另一群是低访问低购买，KMeans 很容易把它们分开。</p><p>KMeans 的问题也很明显：</p><ul><li>需要提前指定 $K$</li><li>对初始中心敏感</li><li>对 outlier 敏感</li><li>更适合接近球形的 cluster</li><li>特征 scale 会影响距离，通常要先 normalization</li></ul><h2 id="4-怎么选-K"><a href="#4-怎么选-K" class="headerlink" title="4. 怎么选 K"></a>4. 怎么选 K</h2><p>常见方法是 elbow method 和 silhouette score。</p><p><strong>Elbow method</strong> 看不同 $K$ 下的簇内误差。随着 $K$ 增大，误差一定会下降，因为 cluster 越多，样本离中心越近。但下降幅度会逐渐变小。拐点附近的 $K$ 通常是一个候选。</p><p><strong>Silhouette score</strong> 同时考虑样本离自己 cluster 的距离，以及离其他 cluster 的距离。分数越高，说明样本更像自己 cluster，而不像其他 cluster。</p><p>实际使用时，不要只机械看指标。比如用户分群最后要服务业务策略，那 cluster 数量还要考虑是否可解释、是否方便运营。</p><h2 id="5-DBSCAN：基于密度的聚类（clustering）"><a href="#5-DBSCAN：基于密度的聚类（clustering）" class="headerlink" title="5. DBSCAN：基于密度的聚类（clustering）"></a>5. DBSCAN：基于密度的聚类（clustering）</h2><p>DBSCAN 不需要提前指定 cluster 数量。它通过密度来定义 cluster。</p><p>两个重要参数：</p><ul><li><code>eps</code>：邻域半径</li><li><code>min_samples</code>：成为核心点需要的邻居数量</li></ul><p>DBSCAN 会把点分成三类：</p><table><thead><tr><th>类型</th><th>含义</th></tr></thead><tbody><tr><td>Core point</td><td>eps 范围内有足够多邻居</td></tr><tr><td>Border point</td><td>不满足 core 条件，但在某个 core point 邻域内</td></tr><tr><td>Noise point</td><td>不属于任何密度区域</td></tr></tbody></table><p>DBSCAN 的优点是能发现非球形 cluster，也能识别 outlier。</p><p>比如二维空间里有两个弯月形 cluster，KMeans 可能分不好，因为它偏向球形；DBSCAN 更可能按密度连通性分出来。</p><p>缺点是参数敏感。如果 <code>eps</code> 太小，很多点会被当成 noise；如果太大，不同 cluster 可能被连在一起。</p><h2 id="6-层次聚类（hierarchical-clustering）"><a href="#6-层次聚类（hierarchical-clustering）" class="headerlink" title="6. 层次聚类（hierarchical clustering）"></a>6. 层次聚类（hierarchical clustering）</h2><p>Hierarchical clustering 关注层级结构。最常见的是 agglomerative clustering，也就是自底向上合并。</p><p>流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">start with each point as its own cluster<br>merge the two closest clusters<br>repeat until only one cluster remains or target number reached<br></code></pre></td></tr></table></figure><p>它的结果可以用 dendrogram 表示。虽然这里不画图，但可以把它想成一棵树：底部是单个样本，越往上 cluster 越大。</p><p>关键在于怎么定义 cluster 之间的距离：</p><ul><li>single linkage：两个 cluster 中最近点的距离</li><li>complete linkage：两个 cluster 中最远点的距离</li><li>average linkage：所有点对距离的平均</li><li>Ward linkage：合并后簇内方差增加最小</li></ul><p>Hierarchical clustering 的优点是能看到层级关系，不一定要一开始就指定 cluster 数。缺点是计算成本高，大数据上不一定适合。</p><h2 id="7-聚类（clustering）方法对比"><a href="#7-聚类（clustering）方法对比" class="headerlink" title="7. 聚类（clustering）方法对比"></a>7. 聚类（clustering）方法对比</h2><table><thead><tr><th>方法</th><th>核心假设</th><th>优点</th><th>缺点</th></tr></thead><tbody><tr><td>KMeans</td><td>cluster 接近球形</td><td>简单、快</td><td>要指定 K，对 outlier 敏感</td></tr><tr><td>DBSCAN</td><td>cluster 是密度区域</td><td>能识别噪声，支持非球形</td><td>参数敏感，高维效果可能差</td></tr><tr><td>Hierarchical</td><td>数据有层级结构</td><td>结果可解释</td><td>大数据成本高</td></tr></tbody></table><p>KMeans 更像是在找中心点，DBSCAN 更像是在找密度连通区域，hierarchical clustering 更像是在整理样本之间的层级关系。</p><h2 id="8-降维（dimensionality-reduction）：为什么要降维"><a href="#8-降维（dimensionality-reduction）：为什么要降维" class="headerlink" title="8. 降维（dimensionality reduction）：为什么要降维"></a>8. 降维（dimensionality reduction）：为什么要降维</h2><p>降维的目标是把高维数据变成低维表示，同时尽量保留重要信息。</p><p>常见动机：</p><ul><li>可视化：把高维 embedding 投到 2D&#x2F;3D</li><li>去噪：去掉不重要的方向</li><li>压缩：减少特征数量</li><li>缓解维度灾难：高维空间中距离可能变得不稳定</li></ul><p>比如一个用户有 1000 个行为特征，很多特征之间高度相关。降维可以把它们压成几十个综合特征，保留主要变化方向。</p><h2 id="9-PCA：用主成分保留主要变化"><a href="#9-PCA：用主成分保留主要变化" class="headerlink" title="9. PCA：用主成分保留主要变化"></a>9. PCA：用主成分保留主要变化</h2><p>PCA 的核心思想是：找到数据方差最大的方向，然后把数据投影到这些方向上。</p><p>基本流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">standardize features<br>compute covariance matrix<br>compute eigenvectors and eigenvalues<br>choose top principal components<br>project data to lower-dimensional space<br></code></pre></td></tr></table></figure><p>如果一个二维数据集大致沿着一条斜线分布，PCA 的第一主成分就是这条斜线方向。把数据投影到这条线，就能用一维保留大部分变化。</p><p>数学上，PCA 会找一组正交方向，第一主成分解释最多 variance，第二主成分在和第一主成分正交的条件下解释剩余最多 variance。</p><h2 id="10-PCA-的几个注意点"><a href="#10-PCA-的几个注意点" class="headerlink" title="10. PCA 的几个注意点"></a>10. PCA 的几个注意点</h2><p>PCA 对 scale 敏感。一个特征范围是 $[0,100000]$，另一个是 $[0,1]$，如果不 standardize，大范围特征会主导 principal components。</p><p>PCA 是线性降维。如果数据本身是复杂非线性结构，PCA 可能表达不好。</p><p>PCA 的主成分不一定好解释。原始特征可能有明确含义，但主成分是多个特征的线性组合，解释起来会更抽象。</p><h2 id="11-t-SNE-和-UMAP"><a href="#11-t-SNE-和-UMAP" class="headerlink" title="11. t-SNE 和 UMAP"></a>11. t-SNE 和 UMAP</h2><p>t-SNE 和 UMAP 常用于可视化高维 embedding，比如把文本 embedding 或图像 embedding 投到二维。</p><p>它们更关注局部邻域结构，适合看样本是否聚在一起。但这类方法主要用于 visualization，不太适合直接当成严肃的全局距离解释。</p><p>一个常见误区是看见二维图上两个 cluster 离得远，就断言原始高维空间里它们也有同样的全局距离关系。这个不一定成立。</p><h2 id="12-几个点"><a href="#12-几个点" class="headerlink" title="12. 几个点"></a>12. 几个点</h2><p>Clustering 没有 label，所以评估本来就更难。指标只能辅助，最后还要看任务目的。</p><p>KMeans 前基本要做 scaling，否则距离会被大尺度特征主导。</p><p>DBSCAN 适合找异常点和非球形 cluster，但参数 <code>eps</code> 很关键。</p><p>PCA 保留的是最大 variance 方向，不等于保留对下游任务最有用的方向。</p><p>降维后的图很好看，不代表结论一定可靠。可视化是探索工具，不是最终证明。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://scikit-learn.org/stable/modules/clustering.html">scikit-learn User Guide: Clustering</a></li><li><a href="https://scikit-learn.org/stable/modules/decomposition.html">scikit-learn User Guide: Decomposition</a></li><li><a href="https://scikit-learn.org/stable/modules/manifold.html">scikit-learn User Guide: Manifold Learning</a></li><li><a href="https://www.statlearning.com/">An Introduction to Statistical Learning</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>机器学习笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>机器学习</tag>
      
      <tag>笔记</tag>
      
      <tag>无监督学习</tag>
      
      <tag>聚类</tag>
      
      <tag>PCA</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>机器学习笔记 04：树模型</title>
    <link href="/2024/06/29/ml-notes-04-tree-based-models/"/>
    <url>/2024/06/29/ml-notes-04-tree-based-models/</url>
    
    <content type="html"><![CDATA[<p>这一篇整理 tree-based models。树模型的核心思想很直观：不断用特征把数据切开，让每个子节点里的样本越来越“纯”。单棵树容易 overfit，所以实际中经常用 ensemble tree models，比如 Random Forest、GBDT、XGBoost。</p><h2 id="1-决策树（decision-tree）的基本想法"><a href="#1-决策树（decision-tree）的基本想法" class="headerlink" title="1. 决策树（decision tree）的基本想法"></a>1. 决策树（decision tree）的基本想法</h2><p>Decision Tree 每一步都在问一个问题：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">这个 feature 按某个 threshold 切开后，数据会不会更容易预测？<br></code></pre></td></tr></table></figure><p>比如判断一个用户是否会购买会员，可以有这样的分裂：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">访问次数 &gt; 10 ?<br>    yes -&gt; 停留时间 &gt; 5 分钟 ?<br>    no  -&gt; 预测不购买<br></code></pre></td></tr></table></figure><p>每个内部节点是一个 split rule，每个叶子节点给出最终预测。</p><p>分类树希望叶子节点里的类别尽量单一；回归树希望叶子节点里的目标值尽量接近。</p><h2 id="2-什么叫节点更“纯”"><a href="#2-什么叫节点更“纯”" class="headerlink" title="2. 什么叫节点更“纯”"></a>2. 什么叫节点更“纯”</h2><p>假设一个节点里有 10 个样本：</p><table><thead><tr><th>类别</th><th align="right">数量</th></tr></thead><tbody><tr><td>正类</td><td align="right">5</td></tr><tr><td>负类</td><td align="right">5</td></tr></tbody></table><p>这个节点很不纯，因为正负各一半。</p><p>如果一次 split 后变成：</p><table><thead><tr><th>子节点</th><th align="right">正类</th><th align="right">负类</th></tr></thead><tbody><tr><td>left</td><td align="right">5</td><td align="right">1</td></tr><tr><td>right</td><td align="right">0</td><td align="right">4</td></tr></tbody></table><p>那这个 split 就不错，因为两个子节点都更容易判断。</p><p>Decision Tree 的 splitting metric，本质上就是量化“split 前后到底变纯了多少”。</p><h2 id="3-熵（entropy）"><a href="#3-熵（entropy）" class="headerlink" title="3. 熵（entropy）"></a>3. 熵（entropy）</h2><p>Entropy 衡量不确定性：</p><p>$$<br>H(S)&#x3D;-\sum_{c}p_c\log_2(p_c)<br>$$</p><p>其中，$p_c$ 是类别 $c$ 在节点中的比例。</p><p>二分类时，如果正负样本各一半，entropy 最大；如果节点里全是同一类，entropy 为 0。</p><p>例子：</p><table><thead><tr><th>节点情况</th><th>Entropy 直觉</th></tr></thead><tbody><tr><td>5 正 &#x2F; 5 负</td><td>不确定性高</td></tr><tr><td>9 正 &#x2F; 1 负</td><td>不确定性低</td></tr><tr><td>10 正 &#x2F; 0 负</td><td>完全纯</td></tr></tbody></table><p>Entropy 越低，节点越纯。</p><h2 id="4-信息增益（information-gain）"><a href="#4-信息增益（information-gain）" class="headerlink" title="4. 信息增益（information gain）"></a>4. 信息增益（information gain）</h2><p>Information Gain 衡量 split 后 entropy 降低了多少：</p><p>$$<br>IG(S,A)&#x3D;H(S)-\sum_{v \in Values(A)}\frac{|S_v|}{|S|}H(S_v)<br>$$</p><p>前半部分是 split 前的不确定性，后半部分是 split 后各子节点不确定性的加权平均。</p><p>如果 split 后子节点变得很纯，后半部分就小，information gain 就大。</p><p>ID3 使用 information gain 来选 split。</p><p>一个直觉例子：用“是否经常访问网站”切用户，如果切完后高访问用户大多购买、低访问用户大多不购买，那么 information gain 就高。如果切完后两边还是正负混杂，那这个 feature 没什么用。</p><h2 id="5-增益率（gain-ratio）"><a href="#5-增益率（gain-ratio）" class="headerlink" title="5. 增益率（gain ratio）"></a>5. 增益率（gain ratio）</h2><p>Information Gain 有一个问题：它偏好取值很多的特征。</p><p>比如用户 ID 这种特征，每个用户几乎都是唯一值。按用户 ID 切，训练集上可以变得很纯，但完全没有泛化意义。</p><p>Gain Ratio 会对这种情况做惩罚：</p><p>$$<br>GainRatio(S,A)&#x3D;\frac{InformationGain(S,A)}{SplitInfo(S,A)}<br>$$</p><p>C4.5 使用 gain ratio。直觉上，它不只看 split 后纯不纯，也看这个 split 是不是把数据切得过于碎。</p><h2 id="6-基尼指数（Gini-index）"><a href="#6-基尼指数（Gini-index）" class="headerlink" title="6. 基尼指数（Gini index）"></a>6. 基尼指数（Gini index）</h2><p>CART 分类树常用 Gini Index：</p><p>$$<br>Gini(S)&#x3D;1-\sum_c p_c^2<br>$$</p><p>Gini 也衡量节点不纯度。节点越纯，Gini 越小。</p><p>二分类时：</p><table><thead><tr><th align="right">正类比例</th><th align="right">Gini</th></tr></thead><tbody><tr><td align="right">0.5</td><td align="right">0.5</td></tr><tr><td align="right">0.9</td><td align="right">0.18</td></tr><tr><td align="right">1.0</td><td align="right">0</td></tr></tbody></table><p>Gini 和 entropy 的目标很接近：都希望 split 后子节点更纯。实际使用中，Gini 计算更简单一些。</p><h2 id="7-ID3、C4-5、CART"><a href="#7-ID3、C4-5、CART" class="headerlink" title="7. ID3、C4.5、CART"></a>7. ID3、C4.5、CART</h2><p>三类经典树可以这样记：</p><table><thead><tr><th>算法</th><th>Split metric</th><th>任务</th></tr></thead><tbody><tr><td>ID3</td><td>Information Gain</td><td>Classification</td></tr><tr><td>C4.5</td><td>Gain Ratio</td><td>Classification</td></tr><tr><td>CART</td><td>Gini &#x2F; Squared Error</td><td>Classification &#x2F; Regression</td></tr></tbody></table><p>CART 是二叉树，每次 split 生成两个子节点。分类时常用 Gini，回归时常用 squared error 或 variance reduction。</p><p>回归树的直觉也很简单：如果一次 split 能让两个子节点内部的目标值更接近，这个 split 就有价值。</p><p>比如预测房价时，按“是否靠近地铁”切分后，如果靠近地铁的一组房价普遍更高，不靠近的一组更低，那么这个 split 就能降低每个子节点内部的误差。</p><h2 id="8-剪枝（pruning）：为什么树需要剪枝"><a href="#8-剪枝（pruning）：为什么树需要剪枝" class="headerlink" title="8. 剪枝（pruning）：为什么树需要剪枝"></a>8. 剪枝（pruning）：为什么树需要剪枝</h2><p>单棵树很容易 overfit。如果不限制深度，它可以不断 split，直到叶子节点里只剩很少样本，甚至每个叶子只对应一个训练样本。</p><p>这在训练集上很好，但对新数据很差。</p><p>常见限制方式：</p><ul><li><code>max_depth</code>：限制树深度</li><li><code>min_samples_split</code>：节点样本太少就不再切</li><li><code>min_samples_leaf</code>：叶子节点至少保留一定样本</li><li><code>max_leaf_nodes</code>：限制叶子节点数</li></ul><p>Pruning 分两类：</p><p><strong>Pre-pruning</strong>：树还没长完就提前停止，比如限制最大深度。</p><p><strong>Post-pruning</strong>：先长出一棵比较完整的树，再从底部往上剪掉泛化收益不大的分支。</p><p>简单说，剪枝就是不让树把训练集记得太细。</p><h2 id="9-随机森林（random-forest）：多棵树投票"><a href="#9-随机森林（random-forest）：多棵树投票" class="headerlink" title="9. 随机森林（random forest）：多棵树投票"></a>9. 随机森林（random forest）：多棵树投票</h2><p>Random Forest 属于 Bagging 方法。它训练很多棵 decision tree，然后让它们投票或取平均。</p><p>Random Forest 的随机性主要来自两点：</p><ol><li>每棵树使用 bootstrap sample，也就是有放回地抽训练样本。</li><li>每次 split 时，只从部分特征中选择最佳 split。</li></ol><p>这样每棵树都不太一样，错误也不会完全一致。</p><p>单棵树 variance 很高，Random Forest 通过多棵树平均来降低 variance。</p><p>一个直觉例子：一个人判断可能很偏，但如果很多个相对独立的人投票，最终结果通常更稳定。Random Forest 也是类似的思路。</p><h2 id="10-提升方法（boosting）、GBDT-和-XGBoost"><a href="#10-提升方法（boosting）、GBDT-和-XGBoost" class="headerlink" title="10. 提升方法（boosting）、GBDT 和 XGBoost"></a>10. 提升方法（boosting）、GBDT 和 XGBoost</h2><p>Boosting 和 Random Forest 不一样。Random Forest 里多棵树大致是并行、独立训练的；Boosting 是一棵接一棵训练，后面的树重点修正前面模型的错误。</p><p>GBDT 的思路是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">initial prediction<br>-&gt; compute residual / negative gradient<br>-&gt; train a new tree to fit the residual<br>-&gt; add this tree to the ensemble<br>-&gt; repeat<br></code></pre></td></tr></table></figure><p>如果第一棵树预测房价总是偏低，下一棵树就会学习这个偏差，把预测往正确方向拉。</p><p>XGBoost 可以看作更工程化、更正则化、更高效的 gradient boosting tree 实现。它常见特点包括：</p><ul><li>使用一阶和二阶梯度信息</li><li>在目标函数中加入正则项</li><li>支持列采样和行采样</li><li>对缺失值有默认 split direction</li><li>工程实现上做了很多并行和缓存优化</li></ul><p>这也是为什么 XGBoost 在传统 tabular data 上经常很强。</p><h2 id="11-几个点"><a href="#11-几个点" class="headerlink" title="11. 几个点"></a>11. 几个点</h2><p>树模型不需要像 KNN 那样强依赖 feature scaling。因为树只关心某个特征是否大于 threshold，不关心距离。</p><p>树模型擅长处理非线性关系和特征交互。比如“访问次数高并且最近 7 天活跃”这种规则，树可以比较自然地表达。</p><p>单棵树容易 overfit。实际项目里更常用 Random Forest、GBDT、XGBoost 这类 ensemble。</p><p>Random Forest 更偏降低 variance，Boosting 更偏逐步降低 bias，但这只是粗略理解，实际效果还和参数、数据、噪声有关。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://scikit-learn.org/stable/modules/tree.html">scikit-learn User Guide: Decision Trees</a></li><li><a href="https://scikit-learn.org/stable/modules/ensemble.html">scikit-learn User Guide: Ensemble Methods</a></li><li><a href="https://www.statlearning.com/">An Introduction to Statistical Learning</a></li><li><a href="https://xgboost.readthedocs.io/">XGBoost Documentation</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>机器学习笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>机器学习</tag>
      
      <tag>笔记</tag>
      
      <tag>决策树</tag>
      
      <tag>随机森林</tag>
      
      <tag>XGBoost</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>机器学习笔记 03：线性模型与 KNN</title>
    <link href="/2024/06/22/ml-notes-03-linear-models-and-knn/"/>
    <url>/2024/06/22/ml-notes-03-linear-models-and-knn/</url>
    
    <content type="html"><![CDATA[<p>这一篇整理监督学习里最基础的几类模型：linear regression、logistic regression 和 KNN。它们看起来简单，但很多后面的模型都能从这里找到影子：线性模型关心如何用特征加权得到预测，logistic regression 把线性打分变成概率，KNN 则完全依赖样本之间的距离。</p><h2 id="1-线性回归（linear-regression）：用线性函数做预测"><a href="#1-线性回归（linear-regression）：用线性函数做预测" class="headerlink" title="1. 线性回归（linear regression）：用线性函数做预测"></a>1. 线性回归（linear regression）：用线性函数做预测</h2><p>Linear regression 用来预测连续值。最简单的一维形式是：</p><p>$$<br>\hat{y} &#x3D; wx + b<br>$$</p><p>多维特征时写成：</p><p>$$<br>\hat{y} &#x3D; w_1x_1 + w_2x_2 + … + w_dx_d + b<br>$$</p><p>比如房价预测中，$x_1$ 是面积，$x_2$ 是房龄，$x_3$ 是距离地铁的距离。模型会学习每个特征对应的权重 $w_i$。如果 $w_1$ 很大，说明面积对预测房价影响更大；如果 $w_2$ 是负数，说明房龄增加可能让房价下降。</p><p>Linear regression 常用 MSE 作为 loss：</p><p>$$<br>J(w,b)&#x3D;\frac{1}{n}\sum_{i&#x3D;1}^{n}(y_i-\hat{y}_i)^2<br>$$</p><p>训练目标就是找到一组 $w,b$，让预测值和真实值之间的平方误差尽量小。</p><p>一个小例子：</p><table><thead><tr><th align="right">面积</th><th align="right">真实房价</th><th align="right">模型预测</th></tr></thead><tbody><tr><td align="right">60</td><td align="right">300</td><td align="right">320</td></tr><tr><td align="right">80</td><td align="right">450</td><td align="right">430</td></tr><tr><td align="right">100</td><td align="right">600</td><td align="right">610</td></tr></tbody></table><p>如果模型预测整体偏高，更新参数时就会降低某些权重或 bias；如果预测整体偏低，就会反过来调大。具体怎么改，由 loss 对参数的 gradient 决定。</p><h2 id="2-线性模型的假设"><a href="#2-线性模型的假设" class="headerlink" title="2. 线性模型的假设"></a>2. 线性模型的假设</h2><p>Linear model 最大的假设是：特征和目标之间可以用线性关系近似。</p><p>这不代表真实世界一定是线性的，而是说在当前特征空间里，线性函数已经足够表达主要趋势。比如房价和面积大致正相关，这种关系线性模型就能抓住。但如果目标和特征之间存在复杂的非线性关系，普通线性模型就会 underfit。</p><p>一个典型例子是广告点击率。用户年龄对点击率的影响可能不是单调线性的：太年轻和太年长的用户都可能低，中间某个年龄段高。直接用年龄做线性特征可能不够，需要特征变换、交叉特征，或者换更强的模型。</p><p>线性模型的优点是简单、稳定、可解释。缺点是表达能力有限，对 feature engineering 比较依赖。</p><h2 id="3-逻辑回归（logistic-regression）：名字叫-regression，但做分类（classification）"><a href="#3-逻辑回归（logistic-regression）：名字叫-regression，但做分类（classification）" class="headerlink" title="3. 逻辑回归（logistic regression）：名字叫 regression，但做分类（classification）"></a>3. 逻辑回归（logistic regression）：名字叫 regression，但做分类（classification）</h2><p>Logistic regression 通常用于二分类。它先计算一个线性打分：</p><p>$$<br>z &#x3D; w^Tx + b<br>$$</p><p>然后用 sigmoid 把这个打分压到 $[0,1]$：</p><p>$$<br>\sigma(z)&#x3D;\frac{1}{1+e^{-z}}<br>$$</p><p>输出可以理解成正类概率：</p><p>$$<br>P(y&#x3D;1|x)&#x3D;\sigma(w^Tx+b)<br>$$</p><p>比如垃圾邮件分类中，如果输出 $0.92$，可以理解为模型认为这封邮件是垃圾邮件的概率很高。如果输出 $0.08$，则更倾向于正常邮件。</p><p>最后再通过 threshold 得到类别：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs text">if P(y = 1 | x) &gt;= 0.5:<br>    predict 1<br>else:<br>    predict 0<br></code></pre></td></tr></table></figure><p>threshold 不一定非要是 0.5。比如疾病筛查里更怕漏诊，可以把 threshold 调低，让模型更容易预测为阳性，从而提高 recall。</p><h2 id="4-为什么逻辑回归（logistic-regression）适合分类"><a href="#4-为什么逻辑回归（logistic-regression）适合分类" class="headerlink" title="4. 为什么逻辑回归（logistic regression）适合分类"></a>4. 为什么逻辑回归（logistic regression）适合分类</h2><p>Linear regression 直接输出连续值，不适合表示概率。Logistic regression 的 sigmoid 输出天然在 $[0,1]$ 范围内，所以可以解释为概率。</p><p>二分类常用 Binary Cross Entropy：</p><p>$$<br>L(y,\hat{y})&#x3D;-[y\log(\hat{y})+(1-y)\log(1-\hat{y})]<br>$$</p><p>如果真实标签是 1，但模型只给正确类别 0.1 的概率，loss 会很大；如果给 0.9，loss 就很小。</p><p>这也是 logistic regression 和 linear regression 的关键区别：</p><table><thead><tr><th>模型</th><th>输出</th><th>常见任务</th><th>常见 loss</th></tr></thead><tbody><tr><td>Linear Regression</td><td>连续值</td><td>Regression</td><td>MSE</td></tr><tr><td>Logistic Regression</td><td>概率</td><td>Binary Classification</td><td>Binary Cross Entropy</td></tr></tbody></table><p>虽然 logistic regression 名字里有 regression，但它本质上是在做分类概率建模。</p><h2 id="5-生成模型（generative-model）和判别模型（discriminative-model）"><a href="#5-生成模型（generative-model）和判别模型（discriminative-model）" class="headerlink" title="5. 生成模型（generative model）和判别模型（discriminative model）"></a>5. 生成模型（generative model）和判别模型（discriminative model）</h2><p>这个分类经常在面试里出现。</p><p><strong>Discriminative model</strong> 直接学习 $P(y|x)$ 或者 decision boundary。它关心的是给定输入 $x$ 后，标签 $y$ 是什么。</p><p><strong>Generative model</strong> 学习联合分布 $P(x,y)$，也可以理解成学习不同类别的数据是怎么生成的。常见写法是：</p><p>$$<br>P(y|x)&#x3D;\frac{P(x|y)P(y)}{P(x)}<br>$$</p><p>Naive Bayes 是典型 generative model，logistic regression 是典型 discriminative model。</p><p>一个简单例子：判断一封邮件是不是垃圾邮件。</p><ul><li>Naive Bayes 会估计“垃圾邮件中出现 free、win、prize 这些词的概率”。</li><li>Logistic regression 更像是直接学习这些词对“垃圾邮件概率”的加权影响。</li></ul><p>简单记法：generative model 更关心数据从哪里来，discriminative model 更关心边界在哪里。</p><h2 id="6-KNN：没有显式训练过程的模型"><a href="#6-KNN：没有显式训练过程的模型" class="headerlink" title="6. KNN：没有显式训练过程的模型"></a>6. KNN：没有显式训练过程的模型</h2><p>KNN 全称是 K-Nearest Neighbors。它的思想很直接：要预测一个新样本，就看训练集中离它最近的 $K$ 个样本，然后让它们投票。</p><p>分类任务里，流程是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">for a new sample:<br>    compute distance to all training samples<br>    choose K nearest samples<br>    count their labels<br>    return the majority label<br></code></pre></td></tr></table></figure><p>比如 $K&#x3D;5$，最近的 5 个邻居里有 3 个是类别 A，2 个是类别 B，那么新样本预测为 A。</p><p>KNN 是 non-parametric model。它没有像 linear regression 那样显式学出一组 $w,b$。训练阶段基本就是存下训练数据，预测阶段才真正计算距离。</p><p>这也带来一个问题：训练很快，但预测可能很慢。数据量很大时，每预测一个样本都要和大量训练样本算距离。</p><h2 id="7-距离和相似度很关键"><a href="#7-距离和相似度很关键" class="headerlink" title="7. 距离和相似度很关键"></a>7. 距离和相似度很关键</h2><p>KNN 的效果很依赖距离度量。常见距离包括 Euclidean distance、Manhattan distance、cosine similarity 等。</p><p>Euclidean distance：</p><p>$$<br>d(x,z)&#x3D;\sqrt{\sum_i (x_i-z_i)^2}<br>$$</p><p>Cosine similarity：</p><p>$$<br>\cos(\theta)&#x3D;\frac{x \cdot z}{|x||z|}<br>$$</p><p>如果特征 scale 差异很大，KNN 会被大尺度特征主导。比如一个特征是年龄 $[0,100]$，另一个特征是收入 $[0,100000]$，不做 normalization 时，收入几乎会决定距离。</p><p>所以 KNN 前通常需要做 feature scaling。</p><h2 id="8-点积（dot-product）和余弦相似度（cosine-similarity）"><a href="#8-点积（dot-product）和余弦相似度（cosine-similarity）" class="headerlink" title="8. 点积（dot product）和余弦相似度（cosine similarity）"></a>8. 点积（dot product）和余弦相似度（cosine similarity）</h2><p>这部分和推荐系统、embedding retrieval、RAG 都有关。</p><p>Dot product 是：</p><p>$$<br>x \cdot z &#x3D; \sum_i x_i z_i<br>$$</p><p>Cosine similarity 是归一化后的方向相似度：</p><p>$$<br>\frac{x \cdot z}{|x||z|}<br>$$</p><p>差别在于：dot product 同时受方向和向量长度影响；cosine similarity 主要看方向。</p><p>如果 embedding 已经归一化，也就是 $|x|&#x3D;|z|&#x3D;1$，那么：</p><p>$$<br>x \cdot z &#x3D; \cos(\theta)<br>$$</p><p>推荐系统里有时会保留 embedding norm，因为热门物品可能被训练出更大的向量长度，dot product 会把这种 popularity signal 也算进去。检索场景里如果只关心语义方向，则常用 cosine similarity 或 normalized embedding dot product。</p><h2 id="9-几个点"><a href="#9-几个点" class="headerlink" title="9. 几个点"></a>9. 几个点</h2><p>Logistic regression 做分类，但它的 decision boundary 仍然是线性的。sigmoid 只是把线性打分转成概率，并没有让边界变复杂。</p><p>KNN 对特征 scale 很敏感。写 KNN 前先想 normalization，不然距离可能没有意义。</p><p>KNN 的 $K$ 太小容易受噪声影响，$K$ 太大又可能把局部结构抹平。</p><p>Linear model 不是“不强”，而是依赖特征表达。如果 feature engineering 做得好，线性模型在很多工业场景里仍然很有用。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://cs229.stanford.edu/">Stanford CS229: Machine Learning</a></li><li><a href="https://scikit-learn.org/stable/modules/linear_model.html">scikit-learn User Guide: Linear Models</a></li><li><a href="https://scikit-learn.org/stable/modules/neighbors.html">scikit-learn User Guide: Nearest Neighbors</a></li><li><a href="https://www.statlearning.com/">An Introduction to Statistical Learning</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>机器学习笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>机器学习</tag>
      
      <tag>笔记</tag>
      
      <tag>线性回归</tag>
      
      <tag>逻辑回归</tag>
      
      <tag>KNN</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>机器学习笔记 02：损失函数、优化与正则化</title>
    <link href="/2024/06/15/ml-notes-02-loss-optimization-regularization/"/>
    <url>/2024/06/15/ml-notes-02-loss-optimization-regularization/</url>
    
    <content type="html"><![CDATA[<p>上一篇笔记里把机器学习看成一个从数据中学习 prediction function 的过程。这篇继续往下看：模型到底是怎么被训练出来的。</p><p>训练过程可以先抓住一条主线：</p><blockquote><p>Loss 定义“错在哪里”，gradient 定义“往哪里改”，learning rate 定义“每次改多少”，regularization 限制模型不要为了降低训练误差而变得过度复杂。</p></blockquote><p>这几个概念连起来，基本就是传统机器学习和深度学习训练的共同底层逻辑。</p><h2 id="1-损失函数（loss-function）：先定义什么叫错"><a href="#1-损失函数（loss-function）：先定义什么叫错" class="headerlink" title="1. 损失函数（loss function）：先定义什么叫错"></a>1. 损失函数（loss function）：先定义什么叫错</h2><p>模型训练不是凭感觉调整参数，而是先把“预测错了多少”变成一个可以计算的数值。这个数值就是 loss。</p><p>不同任务会使用不同 loss，因为它们对“错误”的定义不同。</p><h3 id="MSE：回归任务里最常见的-loss"><a href="#MSE：回归任务里最常见的-loss" class="headerlink" title="MSE：回归任务里最常见的 loss"></a>MSE：回归任务里最常见的 loss</h3><p>Mean Squared Error 常用于回归任务：</p><p>$$<br>J(\theta) &#x3D; \frac{1}{n}\sum_{i&#x3D;1}^{n}(y_i - \hat{y}_i)^2<br>$$</p><p>它惩罚预测值和真实值之间的平方误差。因为误差被平方，大误差会受到更大惩罚。</p><p>例子：</p><table><thead><tr><th align="right">真实值 $y$</th><th align="right">预测值 $\hat{y}$</th><th align="right">误差</th><th align="right">平方误差</th></tr></thead><tbody><tr><td align="right">100</td><td align="right">90</td><td align="right">10</td><td align="right">100</td></tr><tr><td align="right">100</td><td align="right">50</td><td align="right">50</td><td align="right">2500</td></tr></tbody></table><p>第二个预测只比第一个多错 40，但平方误差大很多。这就是 MSE 对大偏差更敏感的原因。</p><p>所以 MSE 的直觉是：不仅希望预测接近真实值，而且特别不希望出现很离谱的预测。</p><h3 id="二元交叉熵（binary-cross-entropy）：二分类任务"><a href="#二元交叉熵（binary-cross-entropy）：二分类任务" class="headerlink" title="二元交叉熵（binary cross entropy）：二分类任务"></a>二元交叉熵（binary cross entropy）：二分类任务</h3><p>二分类中，模型通常输出样本属于正类的概率 $\hat{y}$：</p><p>$$<br>L(y, \hat{y}) &#x3D; -[y\log(\hat{y}) + (1-y)\log(1-\hat{y})]<br>$$</p><p>如果真实标签 $y&#x3D;1$，这个式子会变成 $-\log(\hat{y})$。模型给正确类别的概率越低，loss 越大。</p><p>比如一封邮件确实是垃圾邮件。如果模型预测“是垃圾邮件”的概率是 0.9，loss 很小；如果只给 0.1，loss 会很大。Cross entropy 惩罚的不是类别名本身，而是模型有没有把足够高的概率给到正确类别。</p><p>这和分类任务的目标一致：模型不只是要给出类别，还要把概率质量放到正确类别上。</p><h3 id="多类交叉熵（categorical-cross-entropy）：多分类任务"><a href="#多类交叉熵（categorical-cross-entropy）：多分类任务" class="headerlink" title="多类交叉熵（categorical cross entropy）：多分类任务"></a>多类交叉熵（categorical cross entropy）：多分类任务</h3><p>多分类任务常用 softmax 把 logits 转成概率分布：</p><p>$$<br>\text{softmax}(z_i) &#x3D; \frac{e^{z_i}}{\sum_{j&#x3D;1}^{K}e^{z_j}}<br>$$</p><p>再用 cross entropy 计算损失：</p><p>$$<br>L(y, \hat{y}) &#x3D; -\sum_{j&#x3D;1}^{K} y_j \log(\hat{y}_j)<br>$$</p><p>如果 $y$ 是 one-hot label，这个式子本质上就是取正确类别概率的负对数。</p><p>实现时一般会对 logits 做数值稳定处理，例如先减去最大值：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> numpy <span class="hljs-keyword">as</span> np<br><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">softmax</span>(<span class="hljs-params">logits</span>):<br>    logits = logits - np.<span class="hljs-built_in">max</span>(logits, axis=<span class="hljs-number">1</span>, keepdims=<span class="hljs-literal">True</span>)<br>    exp_logits = np.exp(logits)<br>    <span class="hljs-keyword">return</span> exp_logits / np.<span class="hljs-built_in">sum</span>(exp_logits, axis=<span class="hljs-number">1</span>, keepdims=<span class="hljs-literal">True</span>)<br><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">cross_entropy</span>(<span class="hljs-params">probs, labels</span>):<br>    eps = <span class="hljs-number">1e-12</span><br>    probs = np.clip(probs, eps, <span class="hljs-number">1.0</span> - eps)<br>    correct_probs = probs[np.arange(<span class="hljs-built_in">len</span>(labels)), labels]<br>    <span class="hljs-keyword">return</span> -np.mean(np.log(correct_probs))<br></code></pre></td></tr></table></figure><p>两个容易写错的点：第一，cross entropy 前面有负号；第二，如果输入是 logits，应该先 softmax，而不是直接拿 logits 当概率。</p><h2 id="2-梯度下降（gradient-descent）：模型如何改参数"><a href="#2-梯度下降（gradient-descent）：模型如何改参数" class="headerlink" title="2. 梯度下降（gradient descent）：模型如何改参数"></a>2. 梯度下降（gradient descent）：模型如何改参数</h2><p>有了 loss 后，训练的目标就是找到让 loss 更小的参数。</p><p>Gradient descent 的更新规则是：</p><p>$$<br>\theta_{\text{new}} &#x3D; \theta_{\text{old}} - \alpha \nabla J(\theta)<br>$$</p><p>其中，$\theta$ 是模型参数，$\alpha$ 是 learning rate，$\nabla J(\theta)$ 是 loss 对参数的梯度。</p><p>直觉上，梯度指向 loss 增长最快的方向，所以参数更新要往反方向走。</p><p>训练时可以把流程想成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs text">current parameters<br>-&gt; forward prediction<br>-&gt; compute loss<br>-&gt; compute gradient<br>-&gt; update parameters<br>-&gt; repeat<br></code></pre></td></tr></table></figure><p>Learning rate 很关键。如果太小，训练会很慢；如果太大，参数可能来回震荡，甚至不收敛。</p><p>比如当前参数只需要往左移动一点点就能降低 loss，但 learning rate 太大，一步跨过了最低点，下一步又从另一边跨回来，loss 就可能来回震荡。</p><p>在深度学习里，常见做法是前期 warmup，后期 decay。比如 linear warmup + cosine decay：</p><ul><li>warmup：训练初期逐步增大学习率，避免一开始更新过猛。</li><li>cosine decay：后期逐渐降低学习率，让参数更新更稳定。</li></ul><p>在传统机器学习笔记里不需要展开太多 scheduler 细节，但理解它的作用很有用：learning rate 不是一个纯粹的超参数数字，而是在控制训练动态。</p><h2 id="3-过拟合（overfitting）与欠拟合（underfitting）"><a href="#3-过拟合（overfitting）与欠拟合（underfitting）" class="headerlink" title="3. 过拟合（overfitting）与欠拟合（underfitting）"></a>3. 过拟合（overfitting）与欠拟合（underfitting）</h2><p>训练模型时常见两类问题。</p><p><strong>Underfitting</strong> 指模型太简单，训练集和测试集都表现不好。模型没有学到足够的规律。</p><p><strong>Overfitting</strong> 指模型在训练集上表现很好，但测试集表现差。模型不只是学到了规律，也记住了训练集里的噪声。</p><p>一个常见例子是小数据集上的高阶多项式回归。训练点只有十几个，如果用很高阶的多项式，曲线可以穿过几乎所有训练点，但在训练点之间会剧烈摆动。这种模型看起来 training error 很低，但对新样本不稳定。</p><p>可以用 bias-variance 来理解：</p><table><thead><tr><th>问题</th><th>现象</th><th>直觉</th></tr></thead><tbody><tr><td>High bias</td><td>train&#x2F;test 都差</td><td>模型太简单</td></tr><tr><td>High variance</td><td>train 好，test 差</td><td>模型太依赖训练集</td></tr></tbody></table><p>很多训练技巧其实都是在这两者之间找平衡。模型太弱要提升表达能力，模型太容易记训练集则要增加约束。</p><h2 id="4-正则化（regularization）：限制模型复杂度"><a href="#4-正则化（regularization）：限制模型复杂度" class="headerlink" title="4. 正则化（regularization）：限制模型复杂度"></a>4. 正则化（regularization）：限制模型复杂度</h2><p>Regularization 的作用不是让模型在训练集上 loss 更低，而是限制模型为了降低训练误差而使用过于复杂的参数。</p><p>常见方式是在原本 loss 后面加一个惩罚项：</p><p>$$<br>J_{\text{regularized}}(\theta) &#x3D; J(\theta) + \lambda R(\theta)<br>$$</p><p>其中，$R(\theta)$ 是 regularization term，$\lambda$ 控制正则化强度。</p><h3 id="L1-正则化（L1-regularization）"><a href="#L1-正则化（L1-regularization）" class="headerlink" title="L1 正则化（L1 regularization）"></a>L1 正则化（L1 regularization）</h3><p>L1 正则使用参数绝对值之和：</p><p>$$<br>R(\theta) &#x3D; \sum_i |\theta_i|<br>$$</p><p>它的特点是容易让部分参数变成 0，因此可以产生稀疏性，也常被用于特征选择。</p><p>直觉上，如果一个特征贡献不大，L1 会倾向于直接把它的权重压到 0。</p><h3 id="L2-正则化（L2-regularization）"><a href="#L2-正则化（L2-regularization）" class="headerlink" title="L2 正则化（L2 regularization）"></a>L2 正则化（L2 regularization）</h3><p>L2 正则使用参数平方和：</p><p>$$<br>R(\theta) &#x3D; \sum_i \theta_i^2<br>$$</p><p>它会让参数变小，但通常不会直接变成 0。L2 更像是在鼓励模型使用更平滑、更小的权重。</p><p>很多时候提到的 weight decay 和 L2 regularization 相关，都是通过限制权重大小来降低模型复杂度。</p><table><thead><tr><th>方法</th><th>作用</th><th>结果</th></tr></thead><tbody><tr><td>L1</td><td>惩罚绝对值</td><td>参数更稀疏，部分权重为 0</td></tr><tr><td>L2</td><td>惩罚平方和</td><td>参数更小更平滑，通常不为 0</td></tr></tbody></table><p>简单记法：L1 更像在做“删特征”，L2 更像在做“压权重”。</p><h2 id="5-早停（early-stopping）：用验证集控制训练"><a href="#5-早停（early-stopping）：用验证集控制训练" class="headerlink" title="5. 早停（early stopping）：用验证集控制训练"></a>5. 早停（early stopping）：用验证集控制训练</h2><p>Early stopping 是一种很实用的防过拟合方法。</p><p>训练过程中，如果 validation loss 连续若干轮不再下降，就停止训练。关键是看 validation set，而不是只看 training loss。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">if validation_loss does not improve for patience steps:<br>    stop training<br></code></pre></td></tr></table></figure><p>它的直觉是：当模型继续训练只是在降低 training loss，但 validation loss 开始变差时，模型可能已经开始记训练集噪声了。</p><p>Early stopping 的优点是简单有效，缺点是它会把“什么时候停止训练”也变成一个需要调的策略。</p><h2 id="6-这几个概念如何连在一起"><a href="#6-这几个概念如何连在一起" class="headerlink" title="6. 这几个概念如何连在一起"></a>6. 这几个概念如何连在一起</h2><p>把 loss、optimization 和 regularization 放在一起看，会比单独记定义更清楚。</p><p>Loss 负责定义目标，gradient descent 负责实现参数更新，regularization 负责限制模型复杂度，validation 负责判断模型是不是真的泛化。</p><p>可以按这个顺序理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">loss function: define what is wrong<br>gradient: tell parameters which direction to move<br>learning rate: control how far to move<br>regularization: prevent overly complex parameters<br>validation: check whether the learned pattern generalizes<br></code></pre></td></tr></table></figure><p>机器学习训练最核心的一条线：</p><blockquote><p>训练模型不是单纯让 loss 越低越好，而是在可控复杂度下，让模型学到能迁移到新数据的规律。</p></blockquote><h2 id="7-几个点"><a href="#7-几个点" class="headerlink" title="7. 几个点"></a>7. 几个点</h2><p>Cross entropy 要有负号。因为概率的 log 通常小于等于 0，前面加负号后 loss 才是非负的。</p><p>Sigmoid 的导数是：</p><p>$$<br>\sigma’(x) &#x3D; \sigma(x)(1-\sigma(x))<br>$$</p><p>不是 $f(x)f(1-x)$。</p><p>ROC 曲线里：</p><p>$$<br>\text{TPR} &#x3D; \frac{TP}{TP + FN}<br>$$</p><p>$$<br>\text{FPR} &#x3D; \frac{FP}{FP + TN}<br>$$</p><p>这几个指标后面写 evaluation 的时候再细分，当前先把定义区分开。</p><p>另外，learning rate 大不等于一定能跳出 local minimum。过大的 learning rate 也可能导致训练不稳定或不收敛。更准确的说法是，优化非凸函数时需要结合初始化、学习率策略、优化器和模型结构一起看。</p><h2 id="8-小结"><a href="#8-小结" class="headerlink" title="8. 小结"></a>8. 小结</h2><p>Loss、optimizer、regularization 不应该当成分散知识点，而应该放在一条训练链路里理解：</p><ul><li>loss 决定模型追求什么；</li><li>gradient 决定参数怎么被修改；</li><li>learning rate 决定修改幅度；</li><li>regularization 决定模型不能为了训练集误差任意复杂；</li><li>validation 决定这个训练结果是否值得相信。</li></ul><p>这条链路也能迁移到深度学习和大模型训练里。神经网络复杂很多，但底层仍然是在定义目标、计算梯度、更新参数、控制泛化。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://cs229.stanford.edu/">Stanford CS229: Machine Learning</a></li><li><a href="https://scikit-learn.org/stable/model_selection.html">scikit-learn User Guide: Model selection and evaluation</a></li><li><a href="https://www.statlearning.com/">An Introduction to Statistical Learning</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>机器学习笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>机器学习</tag>
      
      <tag>笔记</tag>
      
      <tag>损失函数</tag>
      
      <tag>优化</tag>
      
      <tag>正则化</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>机器学习笔记 01：机器学习的基本框架</title>
    <link href="/2024/06/08/ml-notes-01-machine-learning-framework/"/>
    <url>/2024/06/08/ml-notes-01-machine-learning-framework/</url>
    
    <content type="html"><![CDATA[<p>这篇笔记整理机器学习最基础的一层框架：机器学习到底在学什么，一个模型从数据到预测结果之间经历了什么，以及为什么训练集表现好并不等于模型真的好。</p><p>可以先把 machine learning 理解成：</p><blockquote><p>从已有数据中学习一个可以泛化到未来数据的 prediction function。</p></blockquote><p>两个关键词：<strong>学习</strong> 和 <strong>泛化</strong>。学习指的是模型通过数据调整自己的参数；泛化指的是模型不只是记住训练样本，而是能在没见过的数据上仍然给出合理预测。</p><h2 id="1-一个机器学习问题由什么组成"><a href="#1-一个机器学习问题由什么组成" class="headerlink" title="1. 一个机器学习问题由什么组成"></a>1. 一个机器学习问题由什么组成</h2><p>最常见的机器学习问题可以写成：</p><p>$$<br>\hat{y} &#x3D; f_\theta(x)<br>$$</p><p>其中，$x$ 是输入特征，$\hat{y}$ 是模型预测，$f_\theta$ 是模型，$\theta$ 是模型参数。</p><p>如果是房价预测，$x$ 可以是面积、地段、楼层、房龄，$y$ 是真实房价。如果是邮件分类，$x$ 可以是邮件文本的特征表示，$y$ 是是否垃圾邮件。如果是推荐系统，$x$ 可以是用户、物品和上下文特征，$y$ 可以是点击、购买或停留时长。</p><p>所以机器学习不是直接“学一个答案”，而是学习一个从输入到输出的映射关系。</p><p>一个很小的例子：</p><table><thead><tr><th>样本</th><th align="right">面积 $x_1$</th><th align="right">房龄 $x_2$</th><th align="right">真实房价 $y$</th></tr></thead><tbody><tr><td>A</td><td align="right">80</td><td align="right">5</td><td align="right">500</td></tr><tr><td>B</td><td align="right">60</td><td align="right">15</td><td align="right">320</td></tr><tr><td>C</td><td align="right">100</td><td align="right">2</td><td align="right">720</td></tr></tbody></table><p>模型可能会先学一个很简单的函数：</p><p>$$<br>\hat{y} &#x3D; w_1x_1 + w_2x_2 + b<br>$$</p><p>训练过程就是不断调整 $w_1$、$w_2$ 和 $b$，让预测房价 $\hat{y}$ 尽量接近真实房价 $y$。</p><p>这个循环就是训练的核心：模型先预测，loss 衡量预测错了多少，optimizer 根据 loss 更新参数，然后模型再预测。</p><h2 id="2-监督学习（supervised-learning）和无监督学习（unsupervised-learning）"><a href="#2-监督学习（supervised-learning）和无监督学习（unsupervised-learning）" class="headerlink" title="2. 监督学习（supervised learning）和无监督学习（unsupervised learning）"></a>2. 监督学习（supervised learning）和无监督学习（unsupervised learning）</h2><p>机器学习通常先按有没有 label 来分。</p><p><strong>Supervised Learning</strong> 有明确的输入和目标输出。模型看到的是 $(x, y)$，目标是学会从 $x$ 预测 $y$。常见任务包括 regression 和 classification。</p><p>Regression 预测连续值，比如房价、温度、点击率。Classification 预测类别，比如垃圾邮件识别、疾病诊断、图片分类。</p><p><strong>Unsupervised Learning</strong> 没有明确的 label。模型只看到 $x$，目标是发现数据内部结构。常见任务包括 clustering、dimensionality reduction 和 density estimation。</p><p>比如 KMeans 会尝试把样本分成若干簇，PCA 会尝试找到数据中方差最大的方向。这类方法不直接回答“这个样本属于哪个真实标签”，而是帮助整理数据结构。</p><p>简单说，supervised learning 更像是在学一个带答案的映射函数；unsupervised learning 更像是在没有标准答案的情况下整理数据的内在结构。</p><h2 id="3-数据集划分为什么重要"><a href="#3-数据集划分为什么重要" class="headerlink" title="3. 数据集划分为什么重要"></a>3. 数据集划分为什么重要</h2><p>机器学习里经常会把数据分成：</p><table><thead><tr><th>数据集</th><th>作用</th></tr></thead><tbody><tr><td>Training set</td><td>用来训练模型参数</td></tr><tr><td>Validation set</td><td>用来调超参数、选模型</td></tr><tr><td>Test set</td><td>用来估计最终泛化能力</td></tr></tbody></table><p>不能只看 training set 表现，因为模型可以通过记住训练样本获得很低的 training loss，但这不代表它真的学到了规律。</p><p>这就是 overfitting 的核心问题：模型在训练集上表现很好，但在新数据上表现很差。</p><p>比如一个垃圾邮件分类器，如果它只是记住了训练集中出现过的垃圾邮件地址，那么训练集 accuracy 可能很高。但新垃圾邮件换了发件人和措辞后，它就可能失效。真正有用的模型应该学习“垃圾邮件通常有哪些模式”，而不是背下训练样本。</p><p>所以 test set 的意义不是“再训练一次”，而是模拟模型未来遇到没见过数据时的表现。</p><h2 id="4-损失函数（loss-function）在做什么"><a href="#4-损失函数（loss-function）在做什么" class="headerlink" title="4. 损失函数（loss function）在做什么"></a>4. 损失函数（loss function）在做什么</h2><p>训练模型前，第一步是定义什么叫“错”。这个定义就是 loss function。</p><p>对于回归任务，常用 Mean Squared Error：</p><p>$$<br>J(\theta) &#x3D; \frac{1}{n}\sum_{i&#x3D;1}^{n}(y_i - \hat{y}_i)^2<br>$$</p><p>其中，$y_i$ 是真实值，$\hat{y}_i$ 是预测值。预测和真实值差得越远，loss 越大。</p><p>比如真实房价是 500，模型预测 450，误差是 50；如果预测 300，误差是 200。MSE 会让第二种错误受到更大的惩罚，因为它离真实值更远。</p><p>对于分类任务，常用 Cross Entropy。它关心的是模型有没有把概率分配给正确类别。</p><p>直觉上，loss function 规定了模型应该为什么事情付出代价。MSE 惩罚数值误差，cross entropy 惩罚正确类别概率太低，hinge loss 则强调分类边界要有 margin。</p><p>因此，不同 loss 不是公式长得不同而已，而是在表达不同的训练目标。</p><h2 id="5-模型训练的本质"><a href="#5-模型训练的本质" class="headerlink" title="5. 模型训练的本质"></a>5. 模型训练的本质</h2><p>机器学习训练可以粗略理解成最小化 empirical risk：</p><p>$$<br>\theta^* &#x3D; \arg\min_\theta \frac{1}{n}\sum_{i&#x3D;1}^{n} L(f_\theta(x_i), y_i)<br>$$</p><p>这句话的意思是：在所有可能的参数 $\theta$ 中，找到一组参数，让模型在训练数据上的平均 loss 尽可能小。</p><p>但关键问题在于：真正关心的不是训练数据上的 loss，而是未来数据上的 loss。训练数据只是总体数据分布的一个样本。</p><p>所以机器学习一直在处理一个张力：</p><ul><li>训练集上 loss 要足够低，否则模型没有学到东西。</li><li>模型不能只适应训练集，否则泛化能力差。</li></ul><p>这也是为什么后面会有 regularization、cross validation、early stopping、model selection 等一整套方法。它们都在服务同一个目标：让模型不仅会做训练题，也能做新题。</p><h2 id="6-一个完整的机器学习流程（ML-pipeline）"><a href="#6-一个完整的机器学习流程（ML-pipeline）" class="headerlink" title="6. 一个完整的机器学习流程（ML pipeline）"></a>6. 一个完整的机器学习流程（ML pipeline）</h2><p>从工程角度看，一个机器学习流程通常不是“选个模型训练一下”这么简单。</p><p>一个典型流程是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs text">data collection<br>-&gt; data cleaning<br>-&gt; feature engineering<br>-&gt; train / validation / test split<br>-&gt; model training<br>-&gt; model selection<br>-&gt; evaluation<br>-&gt; deployment or analysis<br></code></pre></td></tr></table></figure><p>每一步都可能决定最终效果。比如在房价预测里，如果房屋面积单位有的用平方米、有的用平方英尺，而清洗阶段没有统一单位，后面再复杂的模型也会被错误数据带偏。</p><p>数据清洗会影响输入质量，feature engineering 会影响模型能看到什么信息，数据划分会影响评估是否可靠，评价指标会影响最后选哪个模型。</p><p>在实际项目里，模型算法本身经常不是唯一瓶颈。数据质量、特征设计、评估方式、线上线下分布差异，往往同样重要。</p><h2 id="7-小结"><a href="#7-小结" class="headerlink" title="7. 小结"></a>7. 小结</h2><p>一句话概括机器学习的基础框架：</p><blockquote><p>机器学习是先定义一个可学习的函数空间，再用 loss function 指定目标，通过 optimization 找到一组参数，最后用 validation&#x2F;test data 判断它是否真的泛化。</p></blockquote><p>这个框架比记算法名字更重要。KNN、决策树、逻辑回归、XGBoost、神经网络看起来差别很大，但都绕不开几个问题：</p><ul><li>输入特征是什么？</li><li>目标变量是什么？</li><li>模型假设是什么？</li><li>loss 如何定义？</li><li>参数如何学习？</li><li>如何判断模型不是在过拟合？</li></ul><p>后面看具体算法时，也尽量从这些问题出发，而不是只背每个模型的定义。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://cs229.stanford.edu/">Stanford CS229: Machine Learning</a></li><li><a href="https://ocw.mit.edu/courses/6-036-introduction-to-machine-learning-fall-2020/">MIT 6.036 Introduction to Machine Learning</a></li><li><a href="https://scikit-learn.org/stable/user_guide.html">scikit-learn User Guide</a></li><li><a href="https://www.statlearning.com/">An Introduction to Statistical Learning</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>机器学习笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>机器学习</tag>
      
      <tag>监督学习</tag>
      
      <tag>泛化</tag>
      
      <tag>笔记</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
